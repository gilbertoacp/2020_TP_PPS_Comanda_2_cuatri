import { TipoEmpleadoPipe } from './tipo-empleado.pipe';

describe('TipoEmpleadoPipe', () => {
  it('create an instance', () => {
    const pipe = new TipoEmpleadoPipe();
    expect(pipe).toBeTruthy();
  });
});
